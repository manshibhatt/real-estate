first of all set set type module in package.json so 
we can use import a from b

to register first hash password then create user 
nodemon app.js

npx prisma init --data source-provider mongodb

after update npx prisma db push

git clone --single-branch -b branch-name url

axios- request to api
cors - to mak erequestfrom cliemt side something

payload represents user id

nodemon app.js
npm run dev

ls
git add Client
git commit -m "Add Client directory to Git tracking"
git rm --cached -r Client
git commit -m "Remove Client directory from Git index"
git push origin <branch-name>

add client